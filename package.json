{
  "name": "botui-kit",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "pretest": "pnpm -r --sort build",
    "test": "vitest run",
    "typecheck": "pnpm -r --sort typecheck",
    "build": "pnpm -r --sort build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
